using UnityEngine;
using System.Collections;

public class Punto15 : MonoBehaviour {
    
    /*
    // Use this for initialization
    void Start () {
        
        
	}
	
	// Update is called once per frame
	void Update () {
        
	}

    public void onClick() {
        

        end = new Vector3((Random.Range(0, Screen.width)-50), (Random.Range(0, Screen.width - 50)), (Random.Range(0, Screen.width - 50)));
        transform.position = Mathf.Lerp(start.x,end.x,faction);
           
    }

    */
    
}

